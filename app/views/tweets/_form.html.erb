<h2>New Tweet!</h2>
<form class="tweet-compose" data-feed-id="#feed" action="/tweets" method="POST">
  <input
      type="hidden"
      name="authenticity_token"
      value="<%= form_authenticity_token %>">

  <label>
    Content
    <textarea class="tweet-input tweet-input_content" name="tweet[content]"></textarea>
  </label>
  <br>

  <label>
    Mention
    <select class="tweet-input tweet-input_mentions" name="tweet[mentioned_user_ids][]">
      <option></option>
      <% User.all.each do |user| %>
        <option value="<%= user.id %>"><%= user.username %></option>
      <% end %>
    </select>
  </label>
  <br>

  <input class="tweet-input tweet-input_submit" type="Submit" value="Post Tweet!">
</form>
